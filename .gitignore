# OS / editor
.DS_Store
Thumbs.db
Desktop.ini

# Editor settings
.vscode/

# Backend repo (separate)
backend/
# (Removed 'backend/' so backend changes are tracked)

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
**/.env
**/.env.*
!**/.env.example

# Node dependencies (repo root shouldn't have these, but safe)
node_modules/

# Backup archives (large; keep locally)
mbp-backup-*.json
mbp-backup-media-*/
public/mbp-backup-*.json
scripts/.url-map-cache.json

*.swp
